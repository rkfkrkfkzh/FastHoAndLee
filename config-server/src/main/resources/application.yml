server:
  port: 8888  # Config Server 포트 설정

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rkfkrkfkzh/FastHoAndLee.git  # Git 저장소에서 설정 파일을 로드
          branch: master  # master 브랜치 명시
          cloneOnStart: false  # 서버 시작 시 리포지토리를 클론
